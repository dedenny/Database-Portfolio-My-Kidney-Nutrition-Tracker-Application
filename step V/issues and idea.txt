for the lab results date time--very finnicky (need to enter correct time down to seconds, which is unrealistic)
-either some format using html https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/datetime-local
- consider changing the database type (why do we have times down to seconds?  Perhaps just dates would be easier)

Populate edit forms w/ existing data

add a filter/sort feature for each table (e.g. filter dialysis forms from least to greatest by any of the columns displayed)

add a button to patients that brings to additional page showing all the food that they've eaten
